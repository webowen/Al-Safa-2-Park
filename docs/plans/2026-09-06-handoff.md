# 2026-09-06 Handoff

## Project State

- Repo: `webowen/Al-Safa-2-Park`
- Branch: `main`
- Local project path: `G:\AI Agent练习\迪拜AI park\cesium-core-package`
- Stack: Vue 3 + Vite + CesiumJS
- No `three.js` in the main architecture

## Cesium Setup

- Cesium ion model is already wired in
- Asset ID: `5835399`
- Model name: `迪拜AIpark2`
- `window.CESIUM_BASE_URL = "/cesium/"`
- `public/cesium` is copied from `scripts/copy-cesium-assets.mjs`

## Deployment Status

- GitHub push completed successfully
- `README.md` updated with local run and build notes
- Next deployment target: Vercel

## Important Env Vars

```text
VITE_CESIUM_ION_TOKEN=...
VITE_USE_REMOTE_TERRAIN=true
```

## Notes

- Do not commit `.env.local`
- Do not commit `node_modules` or `dist`
- Cesium token should be restricted to the demo asset and allowed deployment URLs
- If the reviewer has no network, ion-based 3D tiles will not load offline

## Next Step

1. Import the GitHub repo into Vercel
2. Set `Root Directory` to `cesium-core-package`
3. Use `npm run build`
4. Use `dist`
5. Verify production URL
